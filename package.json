{
  "name": "elasticsearch-node-rotation",
  "dependencies": {
    "aws-sdk": "^2.264.1",
    "typescript": "2.8.3"
  },
  "devDependencies": {
    "@types/jest": "^23.3.0",
    "@types/node": "8.10.0",
    "jest": "^23.4.1",
    "ts-jest": "^23.0.1"
  },
  "scripts": {
    "compile": "tsc",
    "build": "cd dist && zip -r elasticsearch-node-rotation *",
    "test": "jest"
  }
}
