{
  "name": "elasticsearch-node-rotation",
  "dependencies": {
    "aws-sdk": "^2.264.1",
    "typescript": "2.8.3"
  },
  "devDependencies": {
    "@types/node": "8.10.0"
  },
  "scripts": {
    "compile": "tsc",
    "build": "cd dist && zip -r elasticsearch-node-rotation *"
  }
}
